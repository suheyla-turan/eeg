rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // Tek cihaz / araştırmacı uygulaması — Auth yok.
    // Üretimde App Check veya ağ kısıtı önerilir.
    match /{document=**} {
      allow read, write: if true;
    }
  }
}
